#include "vkz_pyramid.h"

void preparePyramid(PyramidRendering& _pyramid, uint32_t _width, uint32_t _height, uint32_t _levels, uint32_t _level_width, SpecImageViewConfig* _specImageViewConfigs, uint32_t _specImageViewConfigCount)
{
    // create image

    // create shader

    // create program

    // create pass


    _pyramid.width = _width;
    _pyramid.height = _height;
    _pyramid.levels = _levels;
    _pyramid.level_width = _level_width;
    _pyramid.specImageViewConfigs = _specImageViewConfigs;
    _pyramid.specImageViewConfigCount = _specImageViewConfigCount;
}
